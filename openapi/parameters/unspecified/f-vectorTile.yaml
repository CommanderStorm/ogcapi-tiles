name: f
in: query
description: The format of the vector tile response (e.g. json). Accepted values are 'mvt' (Mapbox Vector Tiles), 'mlt' (MapLibre Vector Tiles) or 'json' (GeoJSON).
required: false
schema:
  type: string
  enum:
    - mvt
    - mlt
    - json
style: form
explode: false
